# Data models

No data models yet — the codebase is at scaffold stage (a single exported version constant). Stage data types (BG elements, layers, camera bounds, boundaries) will be documented here once the WASM bridge (backlog item 001) lands.
