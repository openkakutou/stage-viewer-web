# Module: scripts
**Role:** Dev-tooling scripts, outside the shipped app bundle — downloading the `stage` WASM release build.
**Files:** `scripts/download-wasm.mjs`
**Exports:** `downloadWasmRelease(options): Promise<string[]>`, `main(argv, overrides): Promise<number>`, `DownloadError`, `EXIT_CODES`
**Depends on:** (none — a standalone CLI script)
